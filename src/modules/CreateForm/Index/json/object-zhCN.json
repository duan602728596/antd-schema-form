{
  "id": "$root",
  "type": "object",
  "title": "boolean类型",
  "properties": {
    "id": {
      "id": "$root/properties/id",
      "type": "string",
      "title": "当前属性的id",
      "$required": true
    },
    "type": {
      "id": "$root/properties/type",
      "type": "string",
      "title": "数据类型",
      "$required": true,
      "$readOnly": true,
      "$defaultValue": "object"
    },
    "title": {
      "id": "$root/properties/title",
      "type": "string",
      "title": "标题，用于描述关键字的作用",
      "$required": true
    },
    "description": {
      "id": "$root/properties/description",
      "type": "string",
      "title": "说明，用于描述关键字的作用"
    },
    "required": {
      "id": "$root/properties/required",
      "type": "array",
      "title": "对象必须包含的属性",
      "items": {
        "id": "$root/properties/required/items",
        "type": "string",
        "title": "对象必须包含的属性",
        "$required": true
      }
    }
  }
}